apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "synaplan.fullname" . }}-test-connection"
  labels:
    {{- include "synaplan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "synaplan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
