# GPU mode — resource requests and vLLM defaults

vllmDefaults:
  gpu_memory_utilization: 0.85

resources:
  requests:
    cpu: "2"
    memory: "24Gi"
    nvidia.com/gpu: 1
  limits:
    cpu: "4"
    memory: "24Gi"
    nvidia.com/gpu: 1
